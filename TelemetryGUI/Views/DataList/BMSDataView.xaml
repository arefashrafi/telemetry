<UserControl x:Class="TelemetryGUI.Views.DataList.BmsDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TelemetryGUI"
             mc:Ignorable="d"
             Loaded="BmsDataView_OnLoaded"
             Unloaded="BmsDataView_OnUnloaded"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button Name="runButton" Click="RunButton_Click" Grid.Row="0" Width="200">STOP</Button>
        <DockPanel Grid.Row="1">
            <ContentControl>
                <DataGrid SelectionChanged="DataGrid_SelectionChanged" x:Name="DataGrid" ItemsSource="{Binding BMSs}"
                          VerticalContentAlignment="Stretch"
                          HorizontalContentAlignment="Stretch" VerticalAlignment="Stretch"
                          HorizontalAlignment="Stretch" Grid.ColumnSpan="2" />
            </ContentControl>
        </DockPanel>
    </Grid>
</UserControl>