<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dataList="clr-namespace:TelemetryGUI.ViewModel.DataList" x:Class="TelemetryGUI.Views.DataList.MpptDataView"
    mc:Ignorable="d"

    d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.DataContext>
            <dataList:MpptViewModel />
        </Grid.DataContext>
        <DockPanel Grid.Row="1">
            <ContentControl>
                <DataGrid SelectionChanged="DataGrid_OnSelectionChanged" x:Name="DataGrid"
                          ItemsSource="{Binding MppTs}"
                          VerticalContentAlignment="Stretch"
                          HorizontalContentAlignment="Stretch" VerticalAlignment="Stretch"
                          HorizontalAlignment="Stretch" />
            </ContentControl>
        </DockPanel>
    </Grid>
</UserControl>