<UserControl x:Class="TelemetryGUI.Views.DataList.MotorDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Loaded="MotorDataView_OnLoaded"
             Unloaded="MotorDataView_OnUnloaded"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <DockPanel>
            <ContentControl>
                <DataGrid x:Name="DataGrid" SelectionChanged="DataGrid_SelectionChanged" ItemsSource="{Binding Motors}"
                          VerticalContentAlignment="Stretch"
                          HorizontalContentAlignment="Stretch" VerticalAlignment="Stretch"
                          HorizontalAlignment="Stretch" Grid.ColumnSpan="2" />
            </ContentControl>
        </DockPanel>
    </Grid>
</UserControl>