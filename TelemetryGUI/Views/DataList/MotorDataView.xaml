<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:DataList="clr-namespace:TelemetryGUI.ViewModel.DataList" x:Class="TelemetryGUI.Views.DataList.MotorDataView"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.DataContext>
            <DataList:MotorViewModel />
        </Grid.DataContext>
        <DockPanel>
            <ContentControl>
                <DataGrid x:Name="DataGrid" SelectionChanged="DataGrid_SelectionChanged" ItemsSource="{Binding Motors}"
                          VerticalContentAlignment="Stretch"
                          HorizontalContentAlignment="Stretch" VerticalAlignment="Stretch"
                          HorizontalAlignment="Stretch" Grid.ColumnSpan="2" />
            </ContentControl>
        </DockPanel>
    </Grid>
</UserControl>